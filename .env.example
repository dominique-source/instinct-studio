# Public site URL, used for metadata, Open Graph tags and the sitemap.
NEXT_PUBLIC_SITE_URL=https://instinctstudio.studio

# Server-only. Where the Collaborate form forwards submissions (JSON POST).
# Leave unset to show the email fallback instead of submitting anywhere.
CONTACT_FORM_ENDPOINT=
